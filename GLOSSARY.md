# Глосарій — усі скорочення й терміни парсера abit-poisk

Довідка для розшифровки того, що видає CLI. Розбито на блоки: терміни з самого сайту abit-poisk (шапка сторінки, колонки таблиці), і терміни, які ввела ця тула (вердикт v1, градація v2).

---

## 1. Шапка напряму (з сайту)

Рядок виду `ВМ 90 • БМmax 44 • БМmin 5 • К 45 • Заяв 153 • Конкурс на бюджет 3.25`

| Скорочення | Повна назва | Що означає |
|---|---|---|
| **ВМ** | Всього місць | Загальна кількість місць на напрямі — бюджет + контракт разом |
| **БМmax** (БМ_max) | Бюджетні місця, максимум | Максимально можлива кількість бюджетних місць. У тулі це і є **M** — головне число для вердикту |
| **БМmin** (БМ_min) | Бюджетні місця, мінімум | Гарантований мінімум бюджетних місць. **Присутнє не на кожному напрямі** — якщо на сторінці цього поля нема, тула вважає його відсутнім (`None`), а не нулем |
| **К** | Контрактні місця | Скільки місць виділено на контракт. **Присутнє не завжди** — шапка деяких ВНЗ/спеціальностей його просто не показує. Якщо токена К нема, але є ВМ і БМmax — тула деривує `К = ВМ − БМmax`; якщо й це неможливо, К лишається невідомим (`None`), а контрактна фіча (v4) для цього напряму вимикається з чесним підписом |
| **Заяв** | Кількість заяв | Скільки всього заяв подано на цей напрям (від усіх абітурієнтів, усі пріоритети) |
| **Конкурс на бюджет** | — | Приблизно `Заяв / БМmax` — довідкове число, тула його не використовує в розрахунках |

**Стійкість парсингу шапки:** окрім відсутнього К, шапка різних ВНЗ відрізняється пробілами (звичайні/нерозривні `\xa0`) і тим, чи `К` у тексті — кирилична (У+041A) чи латинська (U+004B) літера, хоча виглядають однаково. Тула нормалізує пробіли й шукає обидва варіанти літери. Єдине поле, без якого парсинг падає — **БМmax**; усі інші (ВМ, К, Заяв, Конкурс) — опційні.

---

## 2. Рядок заявки в рейтинговому списку

Приклад: `1 | Шевчук Д. І. | 1 (Б) | 185.077 | Допущено | — | —`

| Поле | Що означає |
|---|---|
| **№ / позиція** | Порядковий номер у рейтинговому списку (сира позиція — бюджет+контракт разом, усі статуси) |
| **ПІБ** | Прізвище та ініціали абітурієнта |
| **П / Пріоритет** | Яким за черговістю абітурієнт поставив цей напрям серед усіх своїх заяв. `1` = найвищий пріоритет (найбільше хоче саме сюди) |
| **(Б) / (К)** | **Б** = заявка на бюджет, **К** = заявка на контракт. Одна людина часто має ДВА рядки на тому самому напрямі — один (Б), один (К). Тула бере в розрахунок лише (Б)-рядки |
| **Заг.бал / Σ бал** | Конкурсний бал — сума складових (мова, історія, профільний предмет тощо) з ваговими коефіцієнтами. **Важливо:** цей бал НЕ однаковий для тієї самої людини на різних спеціальностях — коефіцієнти різні, тому бал перераховується під кожен напрям окремо |
| **Статус** | `Допущено` / `Відмова` (заявку відхилено — людина не рахується конкурентом) / `Заява надійшла з сайту` / `Зареєстровано` |
| **Квота** | Якщо вказано (напр. `Квота-1`) — людина вступає за квотою, поза звичайним конкурсом балів |

---

## 3. Результати пошуку по імені (v2 — `#search-`)

Додаткові поля, які видно тільки в блоці "Крос-аналіз v2":

| Поле | Що означає |
|---|---|
| **ОКР** | Освітньо-кваліфікаційний рівень (напр. `Б` = бакалавр) |
| **ВНЗ** | Вищий навчальний заклад — університет |
| **Спец.** | Спеціальність — код (напр. `F3`) + назва (напр. `Комп'ютерні науки`) |
| **СБО** | Середній бал документа про освіту (атестат/диплом) |
| **Ф** | Факультет / інститут |
| **Д / Оригінали** | Чи подано оригінали документів |
| **direction_id** | Внутрішній ID напряму на сайті (число в URL `/rate2026/direction/{id}`) — тула використовує його, щоб точно ідентифікувати саме ЦЮ заявку серед усіх заявок людини |

---

## 4. Вердикт v1 (широка вилка)

| Термін | Що означає |
|---|---|
| **M** | = БМmax цього напряму. Головне число: скільки бюджетних місць реально є |
| **залізний конкурент** | Абітурієнт вище вас за балом, у якого пріоритет на цей напрям = 1. Вважається, що він майже гарантовано займе бюджетне місце тут (нікуди не піде — це його головний вибір) |
| **група ризику** | Абітурієнт вище вас за балом, у якого пріоритет на цей напрям ≥ 2. Може пройти на вищий пріоритет (інший напрям/виш) і звільнити місце тут |
| **оптимістична межа** | Ваше місце в рейтингу, якщо рахувати конкурентами тільки "залізних" (найкращий для вас сценарій) |
| **песимістична межа** | Ваше місце, якщо рахувати конкурентами і "залізних", і всю "групу ризику" (найгірший сценарій) |
| **ВЕРДИКТ v1** | `ПРОХОДИШ` — навіть песимістична межа ≤ M; `НА МЕЖІ` — оптимістична ≤ M, але песимістична > M; `ПРОЛІТАЄШ` — навіть оптимістична межа > M |

---

## 5. Крос-аналіз v2 (уточнення групи ризику)

Для кожної людини з "групи ризику" тула шукає її ІНШІ заявки з вищим пріоритетом (менше число = вищий пріоритет) і дивиться, чи вона там реально пройде.

| Статус людини | Умова | p_vacate (ймовірність, що звільнить місце тут) |
|---|---|---|
| **точно** (піде) | На вищому пріоритеті її позиція ≤ БМmin того напряму | `1.0` |
| **ймовірно** (піде) | На вищому пріоритеті позиція ≤ БМmax, але > БМmin (або БМmin там не вказано) | `P_LIKELY` (дефолт `0.5`) |
| **лишається** (реальний конкурент) | На всіх вищих пріоритетах позиція > БМmax — нікуди не пройде, лишиться тут | `0.0` |
| **невизначено** | Пошук не дав однозначного результату (мережева помилка, тезка — дублікат пріоритету в результатах, немає вищих пріоритетів взагалі) | не входить у розрахунок "очікуваної", але додається в песимістичну межу як "лишається" |

Підсумкові числа блоку:

| Термін | Формула | Сенс |
|---|---|---|
| **Оптимістична межа** | тверді + "лишається" | Найкращий сценарій: усі "ймовірно/точно" таки пішли |
| **Песимістична межа** | тверді + "лишається" + "ймовірно" + "невизначено" | Найгірший сценарій: ніхто з "ймовірних" не пішов, невизначені теж лишились |
| **Очікувана** | тверді + Σ(1 − p_vacate) по зматчених | Зважене середнє — не гарантія, а орієнтир |

### Подача результату — смуга рангів, не один %

Головне, що виводиться — **смуга рангів проти місць**, песимістичний ранг завжди першим:

> `Найгірший сценарій: 64-й · Очікувано: 46-й · Найкращий: 37-й — місць 44`

Ранги — це те, що реально відомо з чисел; відсотки під ними — евристична надбудова, яка раніше вміла оманливо завищувати (був реальний баг: показувало 67%, хоча очікуваний ранг 45.8 вже був ЗА межею M=44 — бо стара формула інтерполювала між оптимістичною і песимістичною межами, ігноруючи де саме там лежить M). Тепер шанс центрується на самому ліміті (M чи K), тому не може так завищити.

| Термін | Що означає |
|---|---|
| **ВЕРДИКТ** | Чотири стани, з песимістичного кінця: `проходиш` (навіть песимістична межа ≤ ліміт) / `на межі (радше проходиш)` (очікуваний ранг ще ≤ ліміт) / `на межі (радше пролітаєш)` (очікуваний ранг УЖЕ > ліміт, хоч оптимістична межа ще ≤ ліміт) / `пролітаєш` (навіть оптимістична межа > ліміт) |
| **Шанс: песимістичний / очікуваний / оптимістичний** | Три числа, не одне, заголовком завжди песимістичний (найчесніший — 0% у будь-якому "на межі" випадку). Песимістичний і оптимістичний — бінарні (0% чи 100%, чи проходить відповідний сценарій). Очікуваний — єдиний неперервний, центрований на ліміті: 50% рівно коли очікуваний ранг = ліміт |

`P_LIKELY` (дефолт `0.5`, було `0.6`) — довільна ручка, свідомо занижена: краще, щоб "очікуваний" шанс радше занижував, ніж завищував. Калібрується вручну через `--p-likely`, коли з'являться реальні результати вступу й можна буде звірити прогноз з фактом.

---

## 6. Шанс на контракт (v4)

Механіка EDBO (з перших рук, не з сайту): при подачі на **бюджет** вибирають одну з двох опцій — "бюджет" (якщо не проходиш, заявка **автоматично падає в контрактний пул** тієї ж спеціальності) або "контракт-онлі" (одразу і тільки контракт). На abit-poisk сам вибір не показаний — видно лише результат: маркери **(Б)/(К)**.

Рахується **ПІСЛЯ** бюджетного вердикту (v1/v2), бо треба знати, хто з (Б) не пройшов.

| Термін | Що означає |
|---|---|
| **Контрактний пул** | Усі (К)-рядки на цій спеціальності **∪** усі (Б)-рядки, чия позиція в бюджетному рейтингу за балом > M ("не пройшли бюджет"). Дедуп по (ПІБ, бал) — та сама людина може мати і (Б), і (К) рядок з однаковим балом (напр. Мацелко: Пр.5(Б) + Пр.6(К)) — контрактне місце вона займе одне |
| **origin** | Звідки рядок узятий у пул: `контракт` (був явний (К)-рядок) або `бюджет-невдаха` (не пройшла бюджет, впала в контракт автоматично) |
| **K** | З шапки напряму — контрактних місць. Якщо на сторінці лише одне число (без діапазону) — використовується як точне |
| **Ширший критерій вильоту** | Як у v2, але конкурент вище користувача звільняє контрактне місце тут, якщо на вищому пріоритеті ловить **БУДЬ-ЯКЕ** місце — бюджет АБО контракт (не тільки бюджет, як у v2). Тому поріг "ймовірно" — позиція ≤ (БМmax + К) того напряму, а не лише ≤ БМmax |
| **Спрощення "не пройшли бюджет"** | Це проста рангова позиція в (Б)-списку за балом (позиція > M), **без** симуляції вильотів по пріоритету для кожного (Б)-заявника окремо — це роздуло б кількість мережевих запитів на порядки. Вильоти рахуються тільки для пул-учасників вище користувача — той самий принцип, що й у v2 |

Статуси точно/ймовірно/лишається/невизначено, смуга рангів, чотиристанний вердикт і три %-шанси — ті самі, що в блоці 5, лише відносно K замість M. Якщо в шапці немає К (і його не вдалось деривувати з ВМ/БМmax) — кнопка/прапорець контракту вимкнені з підписом "К не вказано в шапці".

---

## 7. CLI-прапорці

| Прапорець | Що робить |
|---|---|
| `--score` | Ваш конкурсний бал |
| `--priority` | Ваш пріоритет заяви на цей напрям |
| `--funding` | `Б` або `К` — на що подаєте саме ви (лише для відображення, на вердикт бюджету не впливає) |
| `--cross-check` / `--no-cross-check` | Вмикає/вимикає крос-аналіз v2 (дефолт: **увімкнено**). З `--no-cross-check` тула не робить жодних додаткових запитів по кожній людині — швидко, як v1 |
| `--contract` | Порахувати шанс на контракт (v4) — окремі мережеві запити по контрактному пулу вище вашого балу. **Не автоматично**, тільки якщо вказано |
| `--p-likely` | Перевизначити ймовірність "ймовірно піде" (дефолт 0.5) — діє і для бюджету, і для контракту |
| `--no-cache` | Не використовувати дисковий кеш результатів пошуку `.cache/search/` |
| `--json` / `--md` | Додатково зберегти результат у файл |
| `--explain` | Переказати результат людською мовою через Anthropic API (потрібен `ANTHROPIC_API_KEY` або `ant auth login`) |
